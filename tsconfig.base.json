{
  "compilerOptions": {
    /* 基本的语言和环境选项 */
    // ↓指定ECMAScript目标版本 "ES3"（默认）， "ES5"， "ES6"/ "ES2015"， "ES2016"， "ES2017"或 "ESNext" (最新版本)。
    "target": "es2022",
    // ↓编译过程中需要引入的库文件的列表。
    "lib": ["ESNext", "DOM", "DOM.Iterable"],
    // ↓指定生成哪个模块系统代码： "None"， "CommonJS"， "AMD"， "System"， "UMD"， "ES6"或 "ES2015"。
    "module": "ESNext",
    // ↓是否编译 javascript 文件，将原样拷贝到编译后的目录
    "allowJs": true,
    // ↓报告 javascript 文件中的错误
    "checkJs": true,
    // ↓设置如何处理.tsx文件
    "jsx": "preserve",
    // ↓决定是否启用从 tslib 库引入语法降级辅助函数,以辅助处理某些功能，如装饰器和 async/await
    "importHelpers": true,
    // ↓忽略所有的声明文件（ *.d.ts）的类型检查，提高编译速度。
    "skipLibCheck": true,
    // ↓将每个文件作为单独模块
    "isolatedModules": true,
    // ↓用于指定是否启用实验性的装饰器特性
    "experimentalDecorators": true,
    // ↓为装饰器提供元数据的支持
    "emitDecoratorMetadata": true,
    // ↓是否将 const enum 结构保留下来
    "preserveConstEnums": true,

    /* 模块解析（编译输入）选项 */
    // ↓解析非相对模块名的基准目录。
    "baseUrl": ".",
    // ↓模块名到基于 baseUrl 的路径映射的列表,"paths"是相对于"baseUrl"进行解析
    "paths": {
      "@custom-ui": ["packages/*"],
      "@custom-ui/utils": ["packages/utils/index.ts"]
    },
    // ↓指定项目编译文件的根目录(输入文件夹)
    "rootDir": ".",
    // ↓要包含的类型声明文件路径列表。
    "typeRoots": ["./node_modules/@types/", "./types"],
    // ↓指定了 TypeScript 解析模块导入的方式
    "moduleResolution": "Node",
    // ↓与 esModuleInterop: true 配合允许从 commonjs 的依赖中直接按 import XX from 'xxx' 的方式导入 default 模块
    "allowSyntheticDefaultImports": true, // 允许从没有设置默认导出的模块中默认导入。这并不影响代码的输出，仅为了类型检查
    "esModuleInterop": true, // 支持合成模块的默认导入
    // 允许引入 .json 模块
    "resolveJsonModule": true,

    /* 编译输出选项 */
    // ↓只进行类型检查而不需要实际编译运行代码
    "noEmit": true,

    /* 严格的类型检查选项 */

    // ↓是否启用所有的严格类型检查选项。
    "strict": true,
    // 相当于启用
    // noImplicitAny, // 是否在表达式和声明上有隐含的 any 类型时报错
    // noImplicitThis, // 当 this 表达式值为 any 类型的时候，生成一个错误
    // alwaysStrict， // 以严格模式检查每个模块，并在每个文件里加入 'use strict'
    // strictNullChecks, // 启用严格的 null 检查, null和 undefined值不包含在任何类型里，只允许用它们自己和 any来赋值（有个例外， undefined可以赋值到 void）。
    // strictFunctionTypes, // 对函数类型的参数进行更严格的类型检查
    // strictPropertyInitialization, // 检查类的实例属性是否已经在构造函数里初始化
    // strictBindCallApply, // 对 bind、call 和 apply 绑定的方法的参数是否严格检测
    // useUnknownInCatchVaria // 设置catch语句捕获的try抛出的返回值类型，从any变成unknown

    /* 额外的检查 */
    // ↓是否在函数缺少返回语句时报告错误
    // "noImplicitReturns": false,
    // ↓用于检查 switch 中是否有 case 没有使用 break 跳出 switch
    "noFallthroughCasesInSwitch": true,
    // ↓检查 import xxx from 'fileName' 语句的文件名和实际文件名是否一致
    "forceConsistentCasingInFileNames": true
  },
  "exclude": ["**/node_modules", "**/__tests__", "**/tests/**", "**/dist"]
}
