{
  // 继承基础配置
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // 用于启用项目的组合编译。通过项目引用(Project References)功能集成到一个 tsconfig.json 中
    "composite": true,
    // 依赖浏览器的 DOM API
    "lib": ["ESNext", "DOM", "DOM.Iterable", "WebWorker"],
    // ↓将 class 声明中的字段语义从 [[Set]] 变更到 [[Define]]
    "useDefineForClassFields": true,
    "types": ["vite/client"]
  },
  "include": [
    "packages",
    "types",
    "**/types",
    // "demos", 设置 demos 则包含文件夹内所有，比如 update-build 内则不需要设置 types /// <reference types="@plugin-web-update-notification/vite" />
    "demos/**/src/**/*.ts",
    "demos/**/src/**/*.vue"
    // "example/src/**/*.vue",
    // "example/src/**/*.ts"
  ]
}
